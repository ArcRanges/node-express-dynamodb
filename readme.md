# NodeJS - DynamoDB Demo

A minimal sample for integrating DynamoDB with NodeJS and perform CRUD functionalities.

- Before running this project, kindly run "npm install" to install project dependencies
